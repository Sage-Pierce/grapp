{
  "name": "grapp",
  "version": "0.0.1",
  "author": "Sage Pierce",
  "license": "MIT",
  "dependencies": {
    "grunt": "~0.4.5",
    "grunt-cli": "^0.1.13"
  },
  "scripts": {
    "postinstall": "(cd codegas-grapp/src/main/webapp && npm install) && (cd codegas-grapp && npm install && grunt)"
  }
}
